# Curation dependencies, pinned to the versions the port was tested with (Python 3.11+; tested on 3.13).
# Install with `python curate/setup.py`, or by hand: python -m venv .venv && .venv/Scripts/pip install -r curate/requirements.txt
Pillow==12.3.0
pillow-heif==1.7.0
numpy==2.5.3
ImageHash==4.3.2
opencv-python-headless==5.0.0.93
onnxruntime==1.29.0
tzdata==2026.3
